function times(secondder) {
  const second = parseFloat(secondder);
  const hours = second / 3600;
  //   console.log("Now it is hours: ", parseInt(hours));
  const seconds = second % 3600;
  const minit = seconds / 60;
  //   console.log("this is minit: ", parseInt(minit));
  const sec = seconds % 60;
  console.log("this is :", sec);
}
times("403000");

// video.others.posted_date

// btn3
const button = document.createElement("button");
button.classList = "btn";
button.innerText = item.category;
btn.append(button);

let n = [35, 4, 5, 5, 6, 6];
n.length;
